import * as React from 'react';
import Button from '@mui/material/Button';
import { ButtonGroup } from '@mui/material';

export default function BasicButtonsGroup() {
  return(
    <ButtonGroup>
      <Button>1</Button>
      <Button>2</Button>
      <Button>3</Button>
    </ButtonGroup>
  )

}
